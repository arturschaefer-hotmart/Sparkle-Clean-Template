package ${packageName}.data.repository

import ${packageName}.domain.repository.${featureRepositoryClass}

class ${featureRepositoryClass} : ${featureRepositoryClass} {
    // TODO: Override the implementation here, that related to the framework
}
