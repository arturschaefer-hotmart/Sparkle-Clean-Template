package ${packageName}.domain.usecase

import ${packageName}.domain.repository.${featureRepositoryClass}

class ${featureUseCaseClass}(private val ${featureRepositoryClass?uncap_first} : ${featureRepositoryClass}) {

    // TODO: This is an example of Use case (usecase between repo and model)
}
